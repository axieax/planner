""" Main Algorithm """
from dataclasses import replace
from data import *

# lookup from data


def place_courses(degree_details: dict, plan_details: dict, plan: list) -> list:
    """
    Pushy

    Args:
        - Current Plan
        - Degree Information
        - Selected Courses
        - Course Overrides (server side validation)
    """
    # filter course requirements
    pass
